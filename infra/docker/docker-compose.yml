services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: where_they_stand
      POSTGRES_USER: wts
      POSTGRES_PASSWORD: localdevelopment
    ports: ["5432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wts -d where_they_stand"]
      interval: 5s
      timeout: 5s
      retries: 10
  redis:
    image: redis:7
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  postgres_data:
