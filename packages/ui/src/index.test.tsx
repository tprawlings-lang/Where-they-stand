import {renderToStaticMarkup} from "react-dom/server";import {describe,expect,it} from "vitest";import {StanceChip} from "./index.js";
describe("rendered stance parity",()=>{it("renders support and opposition with identical structure and attributes",()=>{const normalize=(html:string)=>html.replace(/SUPPORTS|OPPOSES|Supports|Opposes/g,"STANCE");expect(normalize(renderToStaticMarkup(<StanceChip label="SUPPORTS"/>))).toBe(normalize(renderToStaticMarkup(<StanceChip label="OPPOSES"/>)))})});
