import Link from "next/link";import {issueDefinitions} from "@where-they-stand/issue-definitions";
const categories=["Costs and Markets","Government and Elections","Immigration and Border","Rights, Family, and Health","National Security"] as const;
export default function Page(){return <div className="shell page wide"><p className="eyebrow">Fixed 2026 framework</p><h1>The 15 issues</h1><p className="lede">Every candidate is compared against the same approved, versioned questions in this fixed order.</p>{categories.map(category=><section className="issue-group" key={category}><h2>{category}</h2><div className="issue-grid">{issueDefinitions.filter(issue=>issue.category===category).map(issue=><Link className="card issue-link" href={`/issues/${issue.id}`} key={issue.id}><strong>{issue.title}</strong><span>{issue.goal}</span></Link>)}</div></section>)}</div>}
