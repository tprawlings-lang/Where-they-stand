import {RaceSchema} from "@where-they-stand/contracts";import {z} from "zod";import {electionData} from "@/lib/election-data";import {error,pagination,pathValue,query,success,UUIDSchema} from "@/lib/api";
export async function GET(request:Request,{params}:{params:Promise<{electionId:string}>}){const search=query(request,["limit","offset"]);if(!search)return error("INVALID_QUERY_PARAMETER","Unknown query parameter",400);const page=pagination(search);if(!page.success)return error("INVALID_QUERY_PARAMETER","Invalid pagination",400);const {electionId}=await params;if(!pathValue(UUIDSchema,electionId))return error("INVALID_PATH_PARAMETER","Invalid election id",400);if(!await electionData.getElection(electionId))return error("NOT_FOUND","Election not found",404);return success(z.array(RaceSchema),await electionData.listRaces(electionId,page.data))}
